// ﻿//Class
//
// private Vector2 minPosition = new(-1000, -1000);
// private Vector2 maxPosition = new(1000, 1000);
//
// //Method
// {
//     time -= Time.deltaTime;
//     timeRound = Mathf.Round(time);
//
//     if (timeRound == 0)
//     {
//         Vector2 randomPosition = new(
//         Random.Range(minPosition.x, maxPosition.x),
//         Random.Range(minPosition.y, maxPosition.y)
//         );
//
//         rb.velocity = randomPosition * speed * Time.fixedDeltaTime;
//
//         time = Random.Range(1, 10);
//     }
// }
